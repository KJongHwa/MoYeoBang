// eslint-disable-next-line @typescript-eslint/naming-convention
export interface userDTO {
  get: {
    userID: number;
    email: string;
    nickname: string;
    image: string;
    createdAt: string;
    updatedAt: string;
  };
}
